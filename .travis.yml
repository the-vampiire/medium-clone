os: osx
language: node_js
node_js:
  - "10.15.3"
before_install:
  - brew install mongodb-community
  - brew services start mongodb-community
  - npm i
jobs:
  include:
    - stage: test
      script:
        - npm run test:travis
